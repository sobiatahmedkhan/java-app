pipeline {
    agent any

    stages{
        stage ('building project') {
            steps {
                script {
                    sh 'mvn clean  install'
                }
            }
        }

     stage ('creating docker image') {
            steps {
                script {
                    sh 'echo creating docker image'
                }
            }
        }

     stage ('pushing docker image') {
            steps {
                script {
                    sh 'pushing docker image'
                }
            }
        } 
               
    }
}
